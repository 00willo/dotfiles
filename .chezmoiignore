/README.md

# Local backups kept in the source dir for reference; never deploy them.
*.bak

# WIP bootstrap experiment. Target name, not the source filename: .chezmoiignore
# matches target paths. Ignoring it stops the run_onchange script executing,
# so editing dot_bootstrap/setup.yaml cannot trigger an ansible run.
bootstrap.sh

{{ if ne .chezmoi.os "darwin" }}
.config/colima/
Library/LaunchAgents/io.colima.start.plist
{{ end }}
