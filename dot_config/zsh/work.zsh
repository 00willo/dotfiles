# Work-specific config

# allcerts.pem contains system/root certs from Keychain, including proxy root CA.
# export REQUESTS_CA_BUNDLE=/Library/Certificates/allcerts.pem

# Ruby
# eval "$(rbenv init - zsh)"
